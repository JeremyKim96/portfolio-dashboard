# Python
__pycache__/
*.pyc
.venv/
venv/

# 옛 코드 아카이브 (클라우드에 불필요)
_archive_old/

# 에디터/로컬 설정
.claude/

# 로컬 전용 (클라우드에 올리지 않음)
tunnel.log
tunnel2.log
gh_auth.log

# 비밀키 — 절대 커밋 금지 (이게 노출되면 암호문이 풀림)
cloud_data/encrypt.key

# 평문 데이터 — 커밋 금지 (암호문 .enc 만 올림)
cloud_data/portfolio_data.json

# 단, 암호화된 데이터(.enc)는 공개 저장소에 올려도 안전 → 무시하지 않음
!cloud_data/portfolio_data.enc
data/
cloud_data/dashboard_test.html

# OS
Thumbs.db
.DS_Store

# 도구 (대용량 바이너리 — 클라우드에 불필요)
tools/cloudflared.exe

# 비밀 (로컬 secrets)
.streamlit/secrets.toml
